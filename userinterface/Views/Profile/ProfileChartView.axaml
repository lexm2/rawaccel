<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:userinterface.ViewModels.Profile"
             xmlns:chart="using:userinterface.Charting.Controls"
			 xmlns:ext="using:userinterface.Extensions"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="userinterface.Views.Profile.ProfileChartView"
             x:DataType="vm:ProfileChartViewModel">
	<UserControl.Styles>
		<Style Selector="chart|CartesianChart">
			<Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
			<Setter Property="Margin" Value="8"/>
		</Style>
		<Style Selector="Button.chart-control">
			<Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="4"/>
			<Setter Property="Width" Value="32"/>
			<Setter Property="Height" Value="32"/>
			<Setter Property="Padding" Value="4"/>
			<Setter Property="Margin" Value="4"/>
			<Setter Property="Opacity" Value="0.8"/>
		</Style>
		<Style Selector="Button.chart-control:pointerover">
			<Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
			<Setter Property="Opacity" Value="1"/>
		</Style>
		<Style Selector="Button.chart-control.active">
			<Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
			<Setter Property="Opacity" Value="1"/>
		</Style>
		
		<!-- Animations for smooth transitions -->
		<Style Selector="chart|CartesianChart.interactive-chart">
			<Setter Property="Transitions">
				<Transitions>
					<DoubleTransition Property="Opacity" Duration="0:0:0.5"/>
				</Transitions>
			</Setter>
		</Style>
	</UserControl.Styles>
	<Border Background="{DynamicResource HoverBackgroundBrush}"
	        BorderBrush="{DynamicResource BorderBrush}"
	        BorderThickness="1"
	        CornerRadius="8">
		<Grid>
			<!-- Skeleton Loader (shown while chart is initializing) -->
			<Grid IsVisible="{Binding IsLoadingChart}"
			      Background="{DynamicResource ControlBackgroundBrush}"
			      HorizontalAlignment="Stretch"
			      VerticalAlignment="Stretch">
				
				<!-- Chart outline -->
				<Border BorderBrush="{DynamicResource BorderBrush}" 
				        BorderThickness="1" 
				        CornerRadius="4"
				        Margin="20"
				        Opacity="0.3">
					
					<!-- Loading dots -->
					<StackPanel Orientation="Horizontal" 
					           HorizontalAlignment="Center" 
					           VerticalAlignment="Center">
						<Ellipse Width="8" Height="8" 
						        Fill="{DynamicResource AccentBrush}" 
						        Margin="2" 
						        Opacity="0.7"/>
						<Ellipse Width="8" Height="8" 
						        Fill="{DynamicResource AccentBrush}" 
						        Margin="2" 
						        Opacity="0.5"/>
						<Ellipse Width="8" Height="8" 
						        Fill="{DynamicResource AccentBrush}" 
						        Margin="2" 
						        Opacity="0.3"/>
					</StackPanel>
				</Border>
			</Grid>
			
			
			<!-- Interactive Chart (shown after loading) -->
			<chart:CartesianChart x:Name="Chart"
			                    Classes="interactive-chart"
			                    Series="{Binding Series}"
			                    XAxes="{Binding XAxes}"
			                    YAxes="{Binding YAxes}"
			                    TooltipTextPaint="{Binding TooltipTextPaint}"
			                    TooltipBackgroundPaint="{Binding TooltipBackgroundPaint}"
			                    TooltipTextSize="12"
			                    AutoUpdateEnabled="True"
			                    IsVisible="{Binding IsInteractiveMode}"
			                    Opacity="{Binding ChartOpacity}"/>
			
			<!-- Current Mouse Device Display -->
			<StackPanel Orientation="Horizontal"
			            HorizontalAlignment="Left"
			            VerticalAlignment="Top"
			            Margin="16,16,16,2"
			            IsVisible="{Binding IsRealTimeTrackingEnabled}">
				<Border Background="{DynamicResource HoverBackgroundBrush}"
				        BorderBrush="{DynamicResource BorderBrush}"
				        BorderThickness="1"
				        CornerRadius="4"
				        Padding="8,4">
					<StackPanel Orientation="Horizontal">
						<!-- Green dot indicator -->
						<Ellipse Width="8" Height="8"
						         Fill="#FF00C853"
						         VerticalAlignment="Center"
						         Margin="0,0,6,0"/>
						<!-- Device name -->
						<TextBlock Text="{Binding CurrentMouseDevice}"
						           Foreground="{DynamicResource PrimaryTextBrush}"
						           FontSize="12"
						           FontWeight="Medium"
						           VerticalAlignment="Center"/>
						<!-- DPI info -->
						<TextBlock Text=" - "
						           Foreground="{DynamicResource SecondaryTextBrush}"
						           FontSize="12"
						           VerticalAlignment="Center"/>
						<TextBlock Text="{Binding CurrentDeviceDPI}"
						           Foreground="{DynamicResource SecondaryTextBrush}"
						           FontSize="12"
						           VerticalAlignment="Center"/>
					</StackPanel>
				</Border>
			</StackPanel>
			
			<!-- Chart Controls -->
			<StackPanel Orientation="Horizontal"
			            HorizontalAlignment="Right"
			            VerticalAlignment="Bottom"
			            Margin="16,16,16,2"
			            IsVisible="{Binding IsInteractiveMode}">
				<Button Classes="chart-control"
				        Command="{Binding RecreateAxesCommand}"
				        ToolTip.Tip="{ext:Localized ProfileResetZoom}">
					<PathIcon Data="{StaticResource screenshot_regular}"/>
				</Button>
				<Button Classes="chart-control"
				        Command="{Binding FitToDataCommand}"
				        ToolTip.Tip="{ext:Localized ProfileFitToData}">
					<PathIcon Data="{StaticResource full_screen_zoom_regular}"/>
				</Button>
				<Button Classes="chart-control"
				        Classes.active="{Binding IsRealTimeTrackingEnabled}"
				        Command="{Binding ToggleRealTimeTrackingCommand}"
				        ToolTip.Tip="{ext:Localized ProfileRealTimeTracking}">
					<PathIcon Data="{StaticResource top_speed_regular}"/>
				</Button>
				<Button Classes="chart-control"
				        Classes.active="{Binding ShowDriverGraphApproximation}"
				        Command="{Binding ToggleDriverGraphApproximationCommand}"
				        ToolTip.Tip="Show Driver Graph Approximation">
					<PathIcon Data="{StaticResource data_scatter_regular}"/>
				</Button>
			</StackPanel>
		</Grid>
	</Border>
</UserControl>
